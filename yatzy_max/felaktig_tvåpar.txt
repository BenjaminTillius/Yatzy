    if cat == 7:
        pairs = [v+1 for v in range(6) if c[v] >= 2]
        if len(pairs) >= 2:
            return 2*sum(sorted(pairs)[-2:])
        for v in range(6):
            if c[v] >= 2:
                return (v+1) * 2
        return 0

        Det gav: 248.6329